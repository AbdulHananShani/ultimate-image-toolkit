version: '3.8'

services:
  web:
    build: .
    expose:
      - "5000"
    environment:
      - FLASK_ENV=production
    restart: always

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./certs/cloudflare.crt:/etc/ssl/certs/cloudflare.crt
      - ./certs/cloudflare.key:/etc/ssl/private/cloudflare.key
    depends_on:
      - web
    restart: always
